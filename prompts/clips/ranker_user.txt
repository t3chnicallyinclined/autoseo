Episode show: {{show_name}}
Hosts: {{hosts}}
Guest: {{guest}}

TARGET CLIP LENGTH: {{target_secs}}s (acceptable range {{min_secs}}–{{max_secs}}s).
Refined windows MUST end up within this range. Anything that needs longer to
land its hook should score under 40 — there isn't time on the platform.

AUDIENCE TARGET: {{audience}}
  · broad  — pulling new viewers who've never heard the show. Penalize
             inside-baseball, guest-specific references, callbacks to
             prior episodes. Reward universally accessible hooks.
  · core   — rewarding existing fans. Inside jokes are GOOD here. Deep
             cuts and host-quirk callbacks score higher than broad takes.
             Stranger-test (self-contained axis) penalty is relaxed.
  · growth — middle ground. Hook must be broadly accessible but payoff
             can reward the niche. Best for accounts trying to grow.

{{performance_history}}
Below is a JSON array of candidate clip windows from this episode. Each
candidate has:
- index, start_secs, end_secs: where in the episode
- transcript: the words spoken in the window
- linguistic: marker counts + up to 2 quotable lines
- prosody: rms_peak_db / rms_mean_db / f0_mean_hz / f0_variance_hz2 /
  f0_peak_hz / speaking_rate_wps
  · rms_peak_db: peak loudness; less-negative = louder spike
  · f0_variance_hz2: pitch range; higher = more emotional inflection
- novelty: 0..1, higher = unusual topic content relative to the episode mean
- audio_events (optional, omitted when AST is disabled): { laughter,
  applause, music, speech }, each 0..1

{{current_trends}}

Score each candidate per the rubric in the system prompt. Return JSON
exactly matching the schema in the system prompt — one clip entry per
input candidate, indices preserved. Pick hook_type for each.

CANDIDATES:
{{candidates_json}}
